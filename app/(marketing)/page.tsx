export default function LandingPage() {
  return (
    <main className="min-h-screen" style={{ backgroundColor: 'var(--color-concrete-char)' }}>
      <div className="flex items-center justify-center h-screen">
        <p style={{ fontFamily: 'var(--font-display)', fontSize: '1.5rem', letterSpacing: '0.18em', textTransform: 'uppercase', color: 'var(--color-concrete-bone)' }}>
          piramida backstage
        </p>
      </div>
    </main>
  )
}
